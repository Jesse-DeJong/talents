// DOM Targeting
const root = document.getElementById("root");
const points = document.getElementsByClassName("points");

const buildTalentTree = (rows) => {
    
    root.append(tree)
    
    rows.map( row => {
        root.append(row);
    })
}

const buildTalentRow = () => {

}

//const htmlBuilder = (tag, class) => {
//    
//}

switch(e.target.Any(classList.contains())){
    case onepoints:
        classlist.remove("onepoints");
        classlist.add("twopoints");
}

const setPoints = () => {
    points.map( p => {
        p.text 
    })
}